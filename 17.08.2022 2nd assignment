#include<stdio.h>
void main()
{
	int a[30],i,num=1,n,flag;
	printf("enter number:");
	scanf("%d",&n);
	for(i=0;i<n-1;i++)
	scanf("%d",&a[i]);
	for(num=1;num<=n;num++)
	{
		flag=0;
		for(i=0;i<n;i++)
			if(num==a[i])
			{
			flag=1;
			break;	
			}
		if(flag==0)
		printf("%d",num);
	}
}
